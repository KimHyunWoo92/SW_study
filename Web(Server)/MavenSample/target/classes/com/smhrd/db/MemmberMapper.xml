<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- 1. mapper namespace란?  mapper 파일이 여러개일 때, 해당 파일을 식별할 수 있는 식별자
							이름을 정해줄 때는 해당 파일과 연결이 되어야 하는 
							DAO 클래스 이름으로 정해주는 것을 원칙으로 함-->

<mapper namespace="com.smhrd.model.DAO">
	<!-- mapper태그 안쪽에 sql구문을 기술  -->
	<!-- id : DAO에서 SqlSession을 사용해 Mapper파일 안쪽에 있는 sql구문을 찾아줄 때
				사용하는 값으로, 중복 허용하지 않는다!-->
				
				
	<!-- 주의! {}안에는 꼭 필드명으로 맞춰줘야함! 대소문자로 구분한다!  -->
	<insert id="join" parameterType="com.smhrd.model.VO">
	INSERT INTO MEMBER(ID,PW,NICK)
	VALUES(#{id},#{pw},#{nick})
	</insert>
	
	<!-- resultType 동작 원리
		: 나만의 자료형을 resultType 명시해 놓았다면
		: 1. 기본생성자를 호출해서 객체를 생성
		: 2. 조회한 테이블의 컬러명과 일치하는 필드가 있는지 확인
		: 3. 해당 필드의 setter 메소드를 호출해 값을 채우는 구조
	
	  -->
	
	<select id = "login" parameterType="com.smhrd.model.VO" resultType="com.smhrd.model.VO">
	SELECT ID,NICK FROM MEMBER WHERE ID = #{id} and PW = #{pw}
	</select>
  
</mapper>